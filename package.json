{
  "name": "cpp-module-sample",
  "version": "0.0.1",
  "private": false,
  "description": "node cpp(cmake) module example",
  "main": "./index.js",
  "scripts": {
    "build": "rm -fr ./build && node_modules/.bin/ncmake --build ./build"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "node-cmake": "^1.2.0"
  }
}
